#include "Resolution.h"

namespace Darks::IO {

    /*Resolution::Resolution(
        int width,
        int height
    ) : width(width), height(height)
    { }*/

   /* void to_json(nlohmann::json& json, const Resolution& res) {
        json = nlohmann::json{ {"width", res.width}, {"height", res.height} };
    }

    void from_json(const nlohmann::json& json, Resolution& res) {
        json.at("width").get_to(res.width);
        json.at("height").get_to(res.height);
    }   */ 
}